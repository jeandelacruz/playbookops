# Getting Started

Install dependencies with `npm install`, start the explorer with `npm run dev`, and validate the catalog with `npm run validate:catalog`.

Generate adapters with the `generate:*` scripts and a playbook id.

# Validation Model

Validation happens at three levels:

- Schema validation: required fields, enums, and structural contracts.
- Governance validation: permissions, evidence policy, safety rules, risk level, execution mode, owner, status, version, tags, and limitations.
- Behavioral validation: sample task execution or golden output comparison.

Run `npm run validate:catalog` before opening a PR.

